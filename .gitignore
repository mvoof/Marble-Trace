# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
target/

# IDE specific
.continue/
.gemini
.agent
.playwright-mcp
*storybook.log
storybook-static

# Agent configuration. The shared contract is tracked: AGENTS.md and its two
# wrappers, the project skills, the project-level settings and the MCP server
# the visual-testing workflow needs. Personal allow-lists and run state are not.
.claude/*
!.claude/settings.json
!.claude/skills/

# Telemetry snapshots (captured from live iRacing sessions)
# test-data/*.json — tracked in git for CI typecheck / Storybook

# Local python env for scripts/trace-wheel-svg.py
.venv/
__pycache__/

# Agent scratch space (specs, tickets, wayfinding maps)
.scratch/

# Render-budget runs in browser mode (npm run test:perf)
**/__screenshots__/
.vitest-attachments/

# React Doctor writes its report here when asked for one.
react-doctor.json
