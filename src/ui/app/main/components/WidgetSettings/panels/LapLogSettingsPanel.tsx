import { observer } from 'mobx-react-lite';

// Widget ids this panel configures — read by the panel registry.
export const PANEL_WIDGET_IDS = ['lap-log'];

export const LapLogSettingsPanel = observer(() => null);
